import { GenericDashboardPage } from "@/components/eventra/dashboard-templates";

export default function DashboardSettingsPage() {
  return (
    <GenericDashboardPage
      title="Workspace settings"
      description="Notification preferences, session controls, and other account-level settings will live here after auth is connected."
    />
  );
}
