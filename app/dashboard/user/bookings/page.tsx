import { GenericDashboardPage } from "@/components/eventra/dashboard-templates";

export default function UserBookingsPage() {
  return (
    <GenericDashboardPage
      title="My bookings"
      description="Attendees will track payment progress, approval states, and manual proof submission from this booking history page."
    />
  );
}
