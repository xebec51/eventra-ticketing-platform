import { GenericDashboardPage } from "@/components/eventra/dashboard-templates";

export default function OrganizerCheckInPage() {
  return (
    <GenericDashboardPage
      title="Check-in desk"
      description="Search by ticket code, verify ownership, and mark tickets used with duplicate protection and organizer event scope checks."
    />
  );
}
